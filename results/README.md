# Results

This directory keeps compact, publication-friendly result summaries and report notes.

Large per-sample prediction CSVs and logs are intentionally excluded because they may contain benchmark text snippets, model outputs, API usage metadata, or local paths. Recreate those files locally when needed.

